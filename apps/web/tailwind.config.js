// Tailwind 4 (GYM-128): the theme moved CSS-first into src/styles/tokens.css
// (@theme / @theme inline / @utility) and content detection is automatic via
// @tailwindcss/vite — this file is NO LONGER READ by the build and can be
// deleted (kept only because the migration tooling could not remove files).
export default {}
