// Tailwind 4 (GYM-128) runs through @tailwindcss/vite (see vite.config.ts) —
// no PostCSS plugins are needed anymore (v4 vendor-prefixes via Lightning CSS,
// so autoprefixer is gone too). File kept as an explicit no-op.
export default {
    plugins: {},
}
