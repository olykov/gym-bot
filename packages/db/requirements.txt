# Migration tooling for packages/db. Install where migrations are run
# (CI, ops shell, or the api image):  pip install -r packages/db/requirements.txt
#
# SQLAlchemy and psycopg2-binary are pinned explicitly (not left to alembic's
# resolver) so the on-deploy migration container is deterministic — same versions
# every run, no surprise transitive bump. Kept in lockstep with apps/api/requirements.txt.
alembic==1.18.4
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
