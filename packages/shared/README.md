# shared

Shared types, constants, and enums reused across services (e.g. exercise/muscle constants
currently duplicated in `apps/bot` and `apps/api`).

Status: scaffold — to be filled in Phase 2 (see [../../docs/ROADMAP.md](../../docs/ROADMAP.md)).
